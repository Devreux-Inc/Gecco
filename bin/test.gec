

include "mathutils.gec";

class Math {
    add(x, y) {
        return x + y;
    }
}

print Math().add(10, 10);

// This should only print the value without executing the non-exported code in mathutils.gec
print PI;
